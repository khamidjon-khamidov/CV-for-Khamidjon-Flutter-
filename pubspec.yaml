name: cv_for_khamidjon
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  bloc: ^7.2.1
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.3
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  supercharged: ^2.1.1
  google_fonts: ^2.1.0
  logger: ^1.1.0
  rxdart: ^0.27.2
  bloc_concurrency: ^0.1.0
  pull_to_refresh: ^2.0.0

  # path provider
  sembast: ^3.1.1
  path_provider: ^2.0.6
  path: ^1.8.0
  shared_preferences: ^2.0.8
  permission_handler: ^8.3.0
  share_plus: ^3.0.4

  #network
  dio: ^4.0.1
  connectivity_plus: ^2.0.2
  url_launcher: ^6.0.12

  equatable: ^2.0.3
  json_serializable: ^6.0.1
  json_annotation: ^4.3.0
  encrypt: ^5.0.1

  #image
  cached_network_image: ^3.1.0+1
  font_awesome_flutter: ^9.2.0

dev_dependencies:
  bloc_test: ^8.5.0
  flutter_test:
    sdk: flutter
  mocktail: ^0.2.0
  build_runner: ^2.1.5

# flutter pub run build_runner build -> generates json files
#  very_good_analysis: ^2.4.0


flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
    - assets/menu_images/
    - assets/experience_icons/

flutter_intl:
  enabled: true
